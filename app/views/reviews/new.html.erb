<h2>New Review</h2>

<%= simple_form_for([@restaurant, @review]) do |r| %>
  <%= r.input :rating %>
  <%= r.input :content %>
  <%= r.submit %>
<% end %>

<%= link_to "Back to list", restaurants_path%>
